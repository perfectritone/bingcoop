=provide(:title, 'Recipes')

%h1
  = yield :title

#recipes
  =debug(params)
  -#=render partial: 'shared/recipe', collection: @recipes
  render @recipes

-content_for :right_box do
  =render 'shared/recipes_right_bar'
